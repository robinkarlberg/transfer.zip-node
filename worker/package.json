{
  "name": "transfer.zip-node",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "NODE_ENV=development dotenv -e .env -- node server.js",
    "start": "NODE_ENV=production node server.js"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@fastify/jwt": "^9.1.0",
    "@tus/file-store": "^2.0.0",
    "@tus/server": "^2.2.0",
    "fastify": "^5.4.0"
  },
  "devDependencies": {
    "dotenv-cli": "^8.0.0"
  }
}